import { AccountId } from 'caip';
import { AuthMethod } from '@didtools/cacao';
export declare const VERSION = "1";
export declare const CHAIN_NAMESPACE = "stacks";
export declare const chainIdMap: {
    mainnet: string;
    testnet: string;
};
type StacksNetwork = 'mainnet' | 'testnet';
export declare namespace StacksWebAuth {
    /**
     * Get a configured authMethod for an Ethereum account in a web based environment
     */
    function getAuthMethod(stacksProvider: any, account: AccountId): Promise<AuthMethod>;
}
export interface SignatureData {
    signature: string;
    publicKey: string;
}
export type SupportedProvider = {
    signatureRequest: (payload: string) => Promise<SignatureData>;
};
export declare function assertSupportedProvider(stacksProvider: any): asserts stacksProvider is SupportedProvider;
/**
 * Helper function to get an accountId (CAIP10) for an Stacks account by network string 'mainet' | 'testnet'
 */
export declare function getAccountIdByNetwork(network: StacksNetwork, address: string): AccountId;
export {};
