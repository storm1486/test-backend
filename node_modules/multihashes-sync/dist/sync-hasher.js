import { create } from "multiformats/hashes/digest";
export class SyncHasher {
    constructor(name, code, encode) {
        this.name = name;
        this.code = code;
        this.encode = encode;
    }
    digest(input) {
        const result = this.encode(input);
        return create(this.code, result);
    }
}
