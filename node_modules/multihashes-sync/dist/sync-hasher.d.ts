import type { MultihashDigest, SyncMultihashHasher } from "multiformats/hashes/interface";
export declare class SyncHasher<Name extends string, Code extends number> implements SyncMultihashHasher<Code> {
    readonly name: string;
    readonly code: Code;
    private readonly encode;
    constructor(name: string, code: Code, encode: (bytes: Uint8Array) => Uint8Array);
    digest(input: Uint8Array): MultihashDigest<Code>;
}
