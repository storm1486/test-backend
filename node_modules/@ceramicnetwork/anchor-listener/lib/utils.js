import { convertEthHashToCid, getCidFromAnchorEventLog, CONTRACT_TX_TYPE, } from '@ceramicnetwork/anchor-utils';
export function createAnchorProof(chainId, log) {
    return {
        chainId,
        txHash: convertEthHashToCid(log.transactionHash),
        root: getCidFromAnchorEventLog(log),
        txType: CONTRACT_TX_TYPE,
    };
}
//# sourceMappingURL=utils.js.map