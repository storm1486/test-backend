/// <reference types="node" />
import { Writable } from 'stream';
import { Options as RfsOptions } from 'rotating-file-stream';
export declare class SafeStreamHandler {
    ready: boolean;
    protected stream: Writable;
    protected name: string;
    constructor(stream: Writable, name?: string);
    write(message: string): void;
    end(...args: any): void;
    protected writeStream(message: string): void;
}
export declare class RotatingFileStream {
    private filePath;
    private immediate;
    private stream;
    private options;
    constructor(filePath: string, writeImmediately?: boolean, options?: RfsOptions);
    write(message: string): void;
    end(): void;
}
//# sourceMappingURL=stream-helpers.d.ts.map