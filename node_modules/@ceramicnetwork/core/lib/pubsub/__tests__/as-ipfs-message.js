import { serialize } from '../pubsub-message.js';
import * as random from '@stablelib/random';
import { TestUtils } from '@ceramicnetwork/common';
import { peerIdFromCID } from '@libp2p/peer-id';
export function randomPeerId() {
    return peerIdFromCID(TestUtils.randomCID(0, 0x70));
}
export function asIpfsMessage(data, from = randomPeerId()) {
    return {
        type: 'signed',
        from: from,
        topic: 'topic',
        data: serialize(data),
        sequenceNumber: BigInt(random.randomUint32()),
        signature: random.randomBytes(10),
        key: random.randomBytes(10),
    };
}
//# sourceMappingURL=as-ipfs-message.js.map