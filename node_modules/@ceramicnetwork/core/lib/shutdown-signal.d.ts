import { Observer } from 'rxjs';
export declare class ShutdownSignal {
    private subject;
    get observers(): Array<Observer<void>>;
    abort(): void;
    abortable<T>(fn: (abortSignal: AbortSignal) => Promise<T>): Promise<T>;
}
//# sourceMappingURL=shutdown-signal.d.ts.map