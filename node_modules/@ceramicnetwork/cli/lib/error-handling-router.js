import { Router } from '@awaitjs/express';
import { errorHandler } from './daemon/error-handler.js';
export function ErrorHandlingRouter(diagnosticsLogger) {
    const router = Router();
    router.use(errorHandler(diagnosticsLogger));
    return router;
}
//# sourceMappingURL=error-handling-router.js.map