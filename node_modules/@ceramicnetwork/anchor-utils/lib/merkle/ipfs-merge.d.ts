import { Node, type MergeFunction, type TreeMetadata } from './merkle-elements.js';
import type { CIDHolder } from './cid-holder.type.js';
import type { AbortOptions, DiagnosticsLogger } from '@ceramicnetwork/common';
import type { CID } from 'multiformats/cid';
declare type IIpfsService = {
    storeRecord(record: any, options?: AbortOptions): Promise<CID>;
};
export declare class IpfsMerge implements MergeFunction<CIDHolder, TreeMetadata> {
    private readonly ipfsService;
    private readonly logger;
    constructor(ipfsService: IIpfsService, logger: DiagnosticsLogger);
    merge(left: Node<CIDHolder>, right?: Node<CIDHolder> | null, metadata?: TreeMetadata | null): Promise<Node<CIDHolder>>;
}
export {};
//# sourceMappingURL=ipfs-merge.d.ts.map