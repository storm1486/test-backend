import { Node } from '../merkle-elements.js';
export class StringConcat {
    async merge(n1, n2, m) {
        if (m) {
            return new Node(`Hash(${n1} + ${n2} + Metadata(${m}))`, n1, n2);
        }
        else {
            return new Node(`Hash(${n1} + ${n2})`, n1, n2);
        }
    }
}
//# sourceMappingURL=string-concat.js.map